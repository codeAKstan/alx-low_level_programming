#include "main.h"
/**
 *_abs - entry point
 *int - recives a parameter of type int
 *@n: the int to check
 *Return: an absolute type number
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
